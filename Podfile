# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'NBAStatsGo' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    # Pods for NBAStatsGo
    pod 'SwiftyJSON', '~> 4.0'

    target 'NBAStatsGoTests' do
        inherit! :search_paths
        # Pods for testing
    end

end
